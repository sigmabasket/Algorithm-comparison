# Comparative Analysis of Substring Search Algorithms

**Автор:** Зыков Григорий Игоревич (ФТ-101-2)  
**Задача:** Поиск подстроки

## 📋 Описание

Проект предназначен для комплексного сравнительного анализа эффективности различных алгоритмов поиска подстроки. Проводится тестирование по времени выполнения, потреблению памяти, разным сценариям и размерам входных данных.

### Реализованные алгоритмы

- ⚡ **Naive Search** — базовый алгоритм
- 🔤 **Knuth-Morris-Pratt (KMP)** — оптимизированный по времени
- 🏃 **Boyer-Moore** — с использованием таблиц сдвигов
- 🎲 **Rabin-Karp** — на основе хеширования
- 🎯 **Apostolico-Crochemore** — комбинированный подход
- 🔗 **Aho-Corasick** — поиск множественных паттернов

### Методология тестирования

Автоматическое тестирование включает:

| Аспект | Описание |
|--------|---------|
| **Время выполнения** | Со статистикой и доверительными интервалами (95%) |
| **Потребление памяти** | Замер RAM для каждого алгоритма |
| **Сценарии** | Лучший, худший и случайный случаи |
| **Размеры входных данных** | От 1 КБ до 16 МБ |

## 🏗️ Структура проекта

```
├── src/
│   ├── algorithms.py           # Реализация алгоритмов поиска
│   ├── data_generator.py       # Генерация тестовых данных
│   └── __init__.py
│
├── benchmark/
│   ├── benchmark.py            # Главный модуль запуска тестов
│   ├── time_measurer.py        # Замер времени с интервалами
│   ├── memory_measurer.py      # Замер потребления RAM
│   └── __init__.py
│
├── analysis/
│   ├── plot_time_results.py    # Графики производительности
│   ├── plot_memory_results.py  # Графики потребления памяти
│   └── __init__.py
│
├── tests/
│   └── test_all.py             # Тестирование основных методов
│
├── results/
│   ├── results.csv             # Архивированные результаты
│   ├── benchmark.log           # Логирование ошибок и процесса
│   ├── time_results.json       # Результаты по времени (авто)
│   └── memory_results.json     # Результаты по памяти (авто)
│
├── docs/
│   └── substring_report.docx   # Финальный отчёт
│
├── README.md                   # Этот файл
├── requirements.txt            # Зависимости проекта
└── .gitignore                  # Исключения для Git
```

## 🚀 Установка

### Требования

- **Python 3.8+**
- Сторонние библиотеки (установка ниже)

### Установка зависимостей

```bash
pip install -r requirements.txt
```

**Основные модули:**
- `tqdm` — визуализация прогресса
- `matplotlib` — построение графиков
- `numpy` — численные расчёты

## 📊 Использование

### Запуск полного тестирования

```bash
python -m benchmark.benchmark
```

**Особенности работы:**

```
✓ Адаптивный подбор количества запусков (n_runs) в зависимости от размера входа
✓ Прогревочные запуски (5 раз) — исключаются из статистики
✓ Инкрементальное сохранение результатов после каждого блока
✓ Логирование всех ошибок и этапов выполнения
```

**⚠️ Важно:** Перед повторным запуском тестирования очистите JSON файлы в папке `results/`:
```bash
rm results/time_results.json results/memory_results.json
```

### Построение графиков

```bash
python -m analysis.plot_time_results   # Графики времени
python -m analysis.plot_memory_results # Графики памяти
```

### Тестирование методов

```bash
python -m pytest tests/test_all.py -v
```

## 📈 Формат выходных данных

### time_results.json

```json
{
  "naive": {
    "best": [
      {
        "size": 1024,
        "time": 0.00011,
        "delta": 0.00003
      }
    ],
    "worst": [...],
    "random": [...]
  }
}
```

| Поле | Описание |
|------|---------|
| `size` | Длина исходной строки (байты) |
| `time` | Среднее время выполнения (секунды) |
| `delta` | Доверительный интервал 95% |

### memory_results.json

Аналогичный формат, но для потребления памяти в КБ.

## 🔬 Технические особенности

### Статистика

- ✅ **t-таблица** для расчёта доверительных интервалов
- ✅ **Исключение прогревочных запусков** из финальной статистики
- ✅ **Три сценария** для каждого алгоритма (best/worst/random)
- ✅ **Инкрементальная обработка** — сохранение только новых размеров

### Производительность

Программа оптимизирована для:
- Минимизации влияния шума окружения
- Автоматической подстройки параметров
- Эффективного использования памяти

## 📄 Результаты

Полный анализ результатов тестирования и выводы находятся в:

📋 **[substring_report.docx](docs/substring_report.docx)**

Включает:
- Детальное описание каждого алгоритма
- Анализ результатов по сценариям
- Графики зависимостей
- Итоговые рекомендации
